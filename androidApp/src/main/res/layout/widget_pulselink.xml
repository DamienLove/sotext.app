<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_card_frame"
    android:padding="8dp"
    tools:ignore="RemoteViewLayout">

    <!-- Header Row -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="4dp">

        <ImageView
            android:id="@+id/widget_app_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_logo"
            android:contentDescription="@string/app_name" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Beacon"
            android:textColor="@color/widget_gold_light"
            android:textStyle="bold"
            android:textSize="14sp" />

        <!-- New Message Button -->
        <ImageView
            android:id="@+id/widget_compose_button"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:padding="2dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/widget_ic_compose"
            android:tint="@color/widget_gold_light"
            android:contentDescription="@string/action_new_message" />

        <ImageView
            android:id="@+id/widget_settings_button"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:padding="2dp"
            android:src="@drawable/widget_ic_settings"
            android:tint="@color/widget_gold_light"
            android:contentDescription="@string/widget_open_settings" />
    </LinearLayout>

    <!-- Quick Actions Row -->
    <LinearLayout
        android:id="@+id/widget_actions_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_below="@id/widget_header"
        android:paddingBottom="6dp">

        <!-- Emergency Action -->
        <FrameLayout
            android:id="@+id/widget_emergency_action"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:background="@drawable/widget_btn_bg_emergency"
            android:clickable="true">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="SOS"
                android:textColor="@android:color/white"
                android:textStyle="bold" />
        </FrameLayout>

        <!-- Check-In Action -->
        <FrameLayout
            android:id="@+id/widget_checkin_action"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:background="@drawable/widget_btn_bg_checkin"
            android:clickable="true">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="CHECK-IN"
                android:textColor="@android:color/black"
                android:textStyle="bold" />
        </FrameLayout>
    </LinearLayout>

    <!-- Contact Shortcuts Row (Top 4) -->
    <LinearLayout
        android:id="@+id/widget_shortcuts_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_below="@id/widget_actions_row"
        android:paddingBottom="8dp"
        android:gravity="center_horizontal">

        <!-- Items added dynamically -->
    </LinearLayout>

    <!-- Message List -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/widget_shortcuts_row"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <TextView
        android:id="@+id/widget_empty_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="No recent messages"
        android:textColor="#80FFFFFF"
        android:visibility="gone" />

</RelativeLayout>
