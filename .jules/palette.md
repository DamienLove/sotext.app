## 2025-05-15 - Interactive Containers
**Learning:** Users expect "input-like" containers (with icons and borders) to be fully interactive. Making the parent div focus the input on click reduces friction.
**Action:** Always add onClick handlers to custom input wrappers to forward focus to the inner input.
## 2026-03-20 - Focus forward for input wrappers
**Learning:** When creating UX improvements for input-like containers (e.g., custom search bars or password input wrappers), always add an onClick handler to the parent container to forward focus to the inner input, as users expect the entire container to be interactive.
**Action:** Always add onClick handlers to custom input wrappers to forward focus to the inner input.
